#!/usr/bin/env python3 
hostname = input("what val should we set hostname?")

if hostname.lower() == "mtg":
    print("the host name is mtg") 
    print("hostname matches expected config")
else:
    print("the hostname was not mtg")

print("exiting the script")


